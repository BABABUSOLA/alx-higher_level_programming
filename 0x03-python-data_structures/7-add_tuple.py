#!/usr/bin/python3
"""Print functions that adds 2 turples"""


def add_tuple(tuple_a=(), tuple_b=()):
    turple_a += (0, 0)
    turple_b += (0, 0)
    return (turple_a[0] + turple_b[0], turple_a[1] + turple_b[1])
